#자료형을 확인할때는 type() 함수로 찍어보면 된다.
# 복소수? complex??
# a // b 나누고 몫
# a % b 나머지
# a**b 제곱 (a의 b승)
# 문자와 문자열을 구분하지 않음 또한 연산이 불가능하다 (예외로는 문자 + 문자, 문자 * 정수는 가능)
#list 
#tuple : 저장한 값 변경 불가능
#set : 순사 x 중복x
#dictionary 키와 값이 쌍으로 구성 
#형변환은 str() 안에 넣기 int()안에넣기 끝
